<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Miwok">
        <activity android:name=".paginafamily"
            android:parentActivityName=".MainActivity"
            android:label="@string/family_members"/>
        <activity android:name=".paginaphrases"
            android:parentActivityName=".MainActivity"
            android:label="@string/phrases"/>
        <activity android:name=".paginacolors"
            android:parentActivityName=".MainActivity"
            android:label="@string/colors" />
        <activity android:name=".paginanumbers"
            android:parentActivityName=".MainActivity"
            android:label="@string/numbers"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>